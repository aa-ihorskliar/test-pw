name: Run shared

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  trigger-shared:
    name: Trigger shared wf
    uses: agencyanalytics/public-ui/.github/workflows/playwright-tests.yml@main
    with:
      browser: 'chromium'
      project: 'setup'
      stagingId: 'dummy'
