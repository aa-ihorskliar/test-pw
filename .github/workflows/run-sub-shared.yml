name: Run shared

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  trigger-shared:
    name: Trigger shared wf
    uses: aa-ihorskliar/shared-gha-test/sub/.github/workflows/shared3.yml@main
    with:
      browser: 'chromium'
      project: 'setup'
      stagingId: 'dummy'
