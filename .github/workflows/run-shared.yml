name: Run shared

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  trigger-shared:
    name: Trigger shared wf
    uses: agencyanalytics/infrastructure/.github/workflows/playwright-tests.yml@ef93fb2a7791737e5e52358870a7ccf0714e070b
    with:
      browser: 'chromium'
      project: 'setup'
      stagingId: 'dummy'
